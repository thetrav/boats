name: boats
description: turn based multi player old boat combat
version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hexagonal_grid_widget:
    git:
      url: git@github.com:thetrav/hexagonal_grid_widget-master.git
  after_layout: ^1.0.7
  matrix_gesture_detector: ^0.1.0
  vector_math: ^2.0.8
dev_dependencies:
  flutter_test:
    sdk: flutter
  test: 1.13.0


flutter:
  uses-material-design: true
  assets:
    - assets/images/